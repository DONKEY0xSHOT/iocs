"""A fixture used by the standards checker tests."""

# Imports
import os

# Constants
NAME = os.name


# This comment runs on for far too long
# across a second line
# and then a third line which breaks the rule
def one() -> int:
    return 1
